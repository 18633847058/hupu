import 'package:flutter/material.dart';

class ScheduleItem extends StatelessWidget {
  final Match match;

  ScheduleItem(this.match);

  @override
  Widget build(BuildContext context) {
    return null;
  }
}
